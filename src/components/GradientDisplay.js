import React from "react";
import styles from '../styles/GradientDisplay.module.css'

const GradientDisplay = ({data}) => {
    return (
        <div className={styles.gradient}>
            
        </div>
    )
}

export default GradientDisplay